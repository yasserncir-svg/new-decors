services:
  - type: web
    name: new-decors
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ecommerce_complet:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
